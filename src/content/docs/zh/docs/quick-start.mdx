---
title: 快速开始
description: 在本地启动 OctaFuse（Docker 或 Cloudflare 本地 D1），确认健康检查并登录管理台；线上部署见单独章节。
---

import { Aside, Steps } from '@astrojs/starlight/components';

本页只做一件事：**在你自己的电脑上把 OctaFuse 跑起来**，并能打开管理台。更细的「怎么配供应商、路由、Key」见 **[使用指南](/zh/docs/guides/providers/)** 各章节。**生产或预发部署**不在此页展开，见文末链接。

<Aside type="note" title="命令所在仓库">
	以下命令在 **[octafuse-gateway](https://github.com/OctaFuse/octafuse-gateway)** 仓库**根目录**执行。
</Aside>

## 方式 A：Docker（最快跑通）

### 先决条件

- 已安装 **Docker Compose v2.20+**（需支持 `service_completed_successfully`）。
- 可访问 GitHub 以下载源码（或你的 fork）。

### 克隆并启动

```bash
git clone https://github.com/OctaFuse/octafuse-gateway.git
cd octafuse-gateway
docker compose -f docker/compose/quickstart.yml up --build
```

等待容器就绪后，新开终端执行：

```bash
curl -sS http://localhost:8787/health
```

若返回 JSON 且无报错，说明 **Proxy（推理入口）** 已监听 **`http://localhost:8787`**。

## 登录管理台

1. 浏览器打开 **`http://localhost:8789`**（**Admin**）。
2. 使用默认账号登录：**`admin` / `changeme`**（**仅限本地体验**；任何对外环境都必须改掉）。

## 建议的下一步（使用指南顺序）

<Steps>
1. [供应商配置](/zh/docs/guides/providers/) — 接入上游模型服务  
1. [模型配置](/zh/docs/guides/models/) — 维护网关识别的模型目录  
1. [路由配置](/zh/docs/guides/routing/) — 把客户端 `model` 映射到上游  
1. [用户管理](/zh/docs/guides/users/) 与 [API Key 管理](/zh/docs/guides/api-keys/) — 谁可以调用、额度多少  
</Steps>

## 可选：立刻打一条测试请求

在管理台里按使用指南配好 **Provider + 路由 + API Key** 后，可将下面命令里的密钥与 `model` 换成你的真实值：

```bash
curl -sS http://localhost:8787/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"model":"your-route-model","messages":[{"role":"user","content":"Hello"}]}'
```

## 方式 B：Cloudflare（本地 D1）

不想跑 Docker、或希望与 **Worker + D1** 生产形态一致时，可在本机用 Wrangler 开发。本地数据持久化在 `./.wrangler/state`。

**先决条件**：**Node.js 20+**、npm。

```bash
git clone https://github.com/OctaFuse/octafuse-gateway.git
cd octafuse-gateway
npm install
npm run db:migrate          # 本地 D1 迁移
npm run dev:proxy           # Proxy → http://127.0.0.1:8787
```

另开终端：

```bash
npm run dev:admin           # Admin 预览 → http://127.0.0.1:8789
```

管理 API 的 Bearer 须与 D1 `system_config.MASTER_KEY` 一致（开发种子见仓库 `packages/core/migrations-d1/0002_seed.sql`）。

更多本地组合（Node + Postgres/MySQL、多套 D1 目录、冒烟脚本）见 [GitHub 技术参考 · local-testing-environments](https://github.com/OctaFuse/octafuse-gateway/blob/main/docs/ops/local-testing-environments.md)。

## 线上环境

- [Docker 部署](/zh/docs/deploy/docker/)  
- [Cloudflare Workers 部署](/zh/docs/deploy/cloudflare/)  
