---
title: API keys
description: Create and manage sk- keys in Admin, inspect status and read-only budget summaries, revoke or delete keys.
---

import { Aside, Steps } from '@astrojs/starlight/components';

Open **User → API Keys**. **API keys** (`sk-…`) authenticate calls to the **Proxy**: **`Authorization: Bearer sk-…`**.

The list shows status, last used time, and a **read-only budget summary** tied to the owning user (so you can sanity-check spend at a glance). When creating a key, choose **attach to an existing user** or **external identity** so the wizard can resolve or create the right user (follow the on-screen steps).

<Aside type="tip" title="Prerequisite">
	Create a [User](/en/docs/guides/users/) first, or use external-identity mode in the key wizard.
</Aside>

<figure class="of-doc-figure">
	<img src="/screenshots/admin-api-keys.png" alt="OctaFuse Admin API Keys page showing key rows, user budget, and status with emails and key content redacted." loading="lazy" />
	<figcaption>API Keys shows key status, owning user, and budget summaries. Emails and key fragments are pixelated in this screenshot.</figcaption>
</figure>

## What to do in Admin

<Steps>
1. Open **API Keys**.
1. **Create**: follow the wizard → pick or enter user info → set a display name → save and **copy** the full `sk-…` immediately (the UI usually warns it is shown only once).
1. **Operate**: review the list; edit, revoke, or delete as needed.
1. **Smoke test**: call **`GET /v1/me`** on your **Proxy** base URL with `Bearer sk-…` and confirm the response matches expectations.
</Steps>

```bash
curl -sS "http://localhost:8787/v1/me" \
  -H "Authorization: Bearer sk-your-api-key"
```

Replace the URL and key with your environment.

## Automation

Use the admin HTTP API from trusted servers — see the [GitHub technical reference](/en/docs/github-reference/).

## Next

- [Request logs](/en/docs/guides/request-logs/)  
- [Integration](/en/docs/guides/integration/)  
