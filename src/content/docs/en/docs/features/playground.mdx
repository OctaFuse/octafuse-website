---
title: Playground
description: Pick one Upstream Target in Admin, edit a JSON body, and probe the upstream without consuming user key budget.
---

import { Aside, Steps } from '@astrojs/starlight/components';

Open **Inference → Playground**. Playground supports Chat, Responses, Images, and other modes, with built-in samples for common LLM, reasoning, and tool-call requests. It sends a **probe** for **one selected Upstream Target** straight to its upstream, so you can verify URLs, credentials, and upstream model names **without** using a customer **`sk-…`** on the normal billing path.

It does **not** write request logs, debit user budgets, or run Route Pool strategy, circuits, and failover. Validate the full behavior with real Proxy calls or [Simulator](/en/docs/features/simulator/). Call Agent Tools directly through `/v1/tools/*`.

<Aside type="tip" title="When to use">
	After editing providers or routes, confirm upstream connectivity here before switching live traffic.
</Aside>

<figure class="of-doc-figure">
	<img src="/screenshots/v2-6-0-playground-tool-stream.webp" alt="OctaFuse Admin Playground with one route selected and the Tool stream request sample loaded." loading="lazy" />
	<figcaption>Select one route on the left, then load a Connectivity, Tool stream, or Reasoning sample in the request panel. The response appears below after Send.</figcaption>
</figure>

## How to use it

<Steps>
1. Open **Playground**.
1. Choose **one route** from the dropdown (icons indicate enabled vs disabled).
1. Choose Chat, Responses, Images, or another available mode and edit the request body; start from a connectivity, reasoning, or tool-call template when helpful. For image generation, pick an **OpenAI** route with Images configured — see [Image generation / edit](/en/docs/guides/images/).
1. Send and inspect status, errors, and body. Streaming requests expose event summaries, terminal state, and available usage; Gemini tool calls also summarize function names and arguments so incomplete streamed arguments are easier to spot.
1. Project-scoped Vertex AI routes use the same service-account parsing and OAuth access-token exchange as Proxy, making Playground suitable for validating the project ID and IAM access.
</Steps>

## Versus Simulator

| Tool | Typical goal |
|------|----------------|
| Playground | Ops/dev: **single Target → upstream** wiring check |
| [Simulator](/en/docs/features/simulator/) | Real **`sk-…`** from the browser to the **Proxy** — auth, routing, budget, logs |

## Next

- [Routing](/en/docs/guides/routing/)  
- [Simulator](/en/docs/features/simulator/)  
